<script id="sub-card" type="text/x-handlebars-template"
{{#each subscriptions}}
<div class="card-body">
  <h5 class="card-title">Subscription:{{subscriptionName}}</h5>
  <p class="card-text">Due Date:{{dueDate}}</date:>
  </p>
  <p class="card-text float-left clearfix">Amount:{{amount}} </p>
  <a id="update" class="btn btn-primary float-right">Update</a>
  <a id="deleteBtn" class="btn btn-danger float-right">X</a>
</div>
{{/each}}